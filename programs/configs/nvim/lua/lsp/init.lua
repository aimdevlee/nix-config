-- LSP Configuration

-- Load LSP handlers (UI customization, hover, etc.)
require("lsp.handlers")

-- Load LSP keymaps
require("lsp.keymaps")

-- Load and configure LSP servers
require("lsp.servers")